# TODO: Update Navbar to Show Selected Section in Single Container

- [x] Add state for activeSection in Dashboard.jsx (default 'home')
- [x] Update sidebar links to buttons with onClick handlers to set activeSection
- [x] Replace horizontal-containers with a single container that conditionally renders content based on activeSection
- [x] Test the functionality by running the app and clicking sidebar items
